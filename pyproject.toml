[tool.poetry]
name = "whatisakatie"
version = "0.1.0"
description = "A personal website built with Flask"
authors = ["Katie <katie.sipos@email.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.0.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
